
(type Vec2 
	x 
	y
	
	(fn mag (vec2)
		(sqrt (+ (pow (.vec2 x) 2) (pow (.vec2 y) 2))))
)

; essentially a scoped namespace
; mag, x and y are all instance variables--> mag gets set to the function on init
