; examples of recursion 

; fibonacci sequence
(set fac [fn (n)
	(if (= n 0)
		1
		(* n (fac (- n 1))))]) 

; factorial function	
(set fib [fn (n)
	(if (<= n 1)
		n
		(+ (fib (- n 1)) (fib (- n 2))))])
		
		