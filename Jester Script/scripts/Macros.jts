
(macro intg (fun var lower upper delta)
	(set area 0.0)
	(set var (float lower))
	
	(loop (< var upper)
		(+= area (* delta fun))
		(+= var delta))
	
	area)
	
(macro lim (fun var point delta)
	(set var (float point))
	(set y1 fun)
	
	(+= var delta)
	(set y2 fun)
	
	(/ (- y2 y1) delta))
	
