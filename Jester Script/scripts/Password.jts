
(function add (a b) 
	(+ a b))

(function gen (len symbols)
	(set password "")
	
	(loop (>= (-- len) 0)
	
		(if symbols (+= password (char (random 33 126)))
			(+= password (char (random 65 90)))))
			
	password)