
(macro randomElem (lst lstLength)
	(nth (random 0 lstLength 1) lst))

(fn gen (length)
	(set password "")
	
	(loop (> length 0)	
		(-- length)
		
		(+= password (char (random 33 126)))		
		password))
