
(defm when (cond result)
	(if (eval cond)
		(eval result)
		nil))
		
(defm for (x from lower below upper)
	(set (eval x) (eval lower))
	(loop (< (eval x) (eval upper))
		(print (eval x))
		(++ (eval x))))