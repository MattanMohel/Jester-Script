
(defm when (cond res)
	(if cond
		res
		nil))
		
#(when true (+ 2 2))

(defm lcomp (for var in list conditional test)
	
	(set result nil)
	
	(iterate var list
		(conditional test
			(append var result)
			nil))
	
	result)
	
(lcomp for x in (1 2 3 4 5) if (equal (mod x 2) 0))