;examples of recursion

"hello"

(function fac (n)
	(if (= n 0)
		1
		(* n (fac (- n 1)))))
		
(function fib (n)
	(if (<= n 1)
		n
		(+ (fib (- n 1)) (fib (- n 2)))))

(macro when (cond result)
	(if cond
		result
		nil))